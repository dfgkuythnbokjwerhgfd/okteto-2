apiVersion: v1
kind: Service
metadata:
  name: giolre
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "giolre"
    port: 8080
  selector:
    app: giolre
