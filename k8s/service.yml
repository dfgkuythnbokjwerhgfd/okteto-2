apiVersion: v1
kind: Service
metadata:
  name: yeroil
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "yeroil"
    port: 8080
  selector:
    app: yeroil
