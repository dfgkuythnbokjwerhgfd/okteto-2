apiVersion: v1
kind: Service
metadata:
  name: peaction
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "peaction"
    port: 8080
  selector:
    app: peaction
